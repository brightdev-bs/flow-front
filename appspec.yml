version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/front
    overwrite: true
    permissions:
      - object: /
        pattern: "**"

hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 300
      runas: root
